# OS noise
.DS_Store

# Secrets and machine-local configuration
/auth.json
/models.json
/keybindings.json
/settings.json
/mcp-cache.json
/trust.json
/update-cache.json
/models-store.json
/usage-tracker-history.json

# Locally built tools
/bin/apply_patch

# Runtime state, local caches, and cloned repositories
/git/
/sessions/
/processes/
/.cache/
/pi-crash.log

# Dependencies, build output, and test coverage
**/node_modules/
**/dist/
**/coverage/
**/.cache/

# pi-peer 运行时名册(会话注册文件,机器本地状态)
/peers/

# agent 本地运行时状态(memory、审计 jsonl、扩展缓存)
/.pi/
.gitignore