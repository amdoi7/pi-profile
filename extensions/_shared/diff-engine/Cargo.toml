[package]
name = "diff-engine"
version = "0.1.0"
edition = "2024"
description = "Rust diff engine for pi extension diff rendering (line diff, line pairing, word highlights)"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
