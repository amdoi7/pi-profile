{
	"name": "pi-worker",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "vitest run tests/*.test.mjs",
		"test:live": "vitest run tests/substrate",
		"typecheck": "tsc -p tsconfig.json"
	},
	"devDependencies": {
		"@types/node": "^26.0.0",
		"typebox": "^1.3.11",
		"typescript": "^5.7.0",
		"vitest": "^3.2.7"
	}
}
