# project-memory skill

The extension registers this skill as the workflow contract for the project work control plane.

Use it when work changes:

- issue scope, ownership, status, acceptance, or task links
- task objective, constraints, result, or evidence
- canonical workspace lessons
- cross-session handoff state

The always-on prompt contains only the compact `Issue -> Task` contract. This skill provides the complete file responsibilities and workflow when needed.
