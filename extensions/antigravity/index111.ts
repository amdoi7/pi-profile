// Entry point for pi auto-discovery (extensions/*/index.ts). The implementation
// lives in src/ so the pure wire/catalog/schema modules stay independently testable.
export { default } from "./src/index.ts";
